import LoginButton from "../Auth0Login";
import LogoutButton from "../Auth0Logout";
const LoginPage = () => {
  return (
    <>
      <LoginButton />
      <LogoutButton />
    </>
  );
};

export default LoginPage;
